"use strict";
describe('Data type', function () {
    it('should must declare', function () {
        const name = "Nico cahya";
        const balance = 999999;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
